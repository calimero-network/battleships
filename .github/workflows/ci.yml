name: CI

on:
  push:
    branches: [master]
    paths:
      - "app/**"
      - "logic/**"
      - "e2e/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - ".github/workflows/ci.yml"

  pull_request:
    branches: [master]
    paths:
      - "app/**"
      - "logic/**"
      - "e2e/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - ".github/workflows/ci.yml"

  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ============================================================
  # Job 1: Frontend — Lint, Typecheck, Build
  # ============================================================
  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: app/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm --dir app install --frozen-lockfile

      - name: Lint
        run: pnpm --dir app lint

      - name: Typecheck
        if: ${{ !cancelled() }}
        run: pnpm --dir app exec tsc --noEmit

      - name: Build
        if: ${{ !cancelled() }}
        run: pnpm --dir app build

      - name: Test
        if: ${{ !cancelled() }}
        run: pnpm --dir app test

  # ============================================================
  # Job 2: Logic — Rust Format, Clippy
  # ============================================================
  # Compile-clean check only — the actual artifact used by e2e is
  # produced in the `bundle` job below, which builds + bundles + uploads.
  logic:
    name: Logic (Rust)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
          components: rustfmt, clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            logic/target
          key: logic-${{ runner.os }}-${{ hashFiles('logic/Cargo.toml', 'logic/crates/*/Cargo.toml') }}
          restore-keys: logic-${{ runner.os }}-

      - name: Cargo format
        working-directory: logic
        run: cargo fmt --check

      - name: Cargo clippy
        if: ${{ !cancelled() }}
        working-directory: logic
        run: cargo clippy --target wasm32-unknown-unknown -- -D warnings

  # ============================================================
  # Job 3: Bundle — build WASM against current core master and emit .mpk
  # ============================================================
  # Cargo.lock is gitignored, so each fresh checkout resolves the
  # `branch = "master"` git deps for calimero-sdk / calimero-storage /
  # calimero-storage-macros / calimero-wasm-abi to whatever is currently
  # at master HEAD. That is exactly what we want: every PR's e2e run is
  # exercised against the up-to-date runtime ABI.
  #
  # The committed `logic/res/battleships-0.3.0.mpk` is for local dev
  # convenience — CI does not consume it. The artifact uploaded here
  # is what the e2e job downloads and installs on the merod nodes.
  #
  # Signing is skipped in CI: the signer (`mero-sign`) lives in the
  # sibling `../../core` checkout which we don't have here. The bundle
  # is unsigned, which `merobox install --e2e-mode` accepts.
  bundle:
    name: Bundle (.mpk)
    runs-on: ubuntu-latest
    needs: [logic]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            logic/target
          key: bundle-${{ runner.os }}-${{ hashFiles('logic/Cargo.toml', 'logic/crates/*/Cargo.toml') }}
          restore-keys: bundle-${{ runner.os }}-

      - name: Build bundle
        working-directory: logic
        run: ./build-bundle.sh

      - name: Upload bundle artifact
        uses: actions/upload-artifact@v4
        with:
          name: battleships-bundle-${{ github.sha }}
          path: logic/res/battleships-*.mpk
          retention-days: 7
          if-no-files-found: error

  # ============================================================
  # Job 4: E2E — Merobox Battleships Workflow
  # ============================================================
  e2e:
    name: E2E (Merobox)
    runs-on: ubuntu-latest
    needs: [bundle]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download freshly built bundle
        uses: actions/download-artifact@v4
        with:
          name: battleships-bundle-${{ github.sha }}
          path: logic/res

      - name: Install merobox (apt)
        run: |
          # Install from the official apt repo (https://calimero-network.github.io/merobox).
          # This is a signed binary release that bundles calimero-client-py internally,
          # so we don't have to coordinate pip versions for the two packages.
          # Mirrors mero-drive's CI setup.
          curl -fsSL https://calimero-network.github.io/merobox/gpg.key \
            | sudo tee /usr/share/keyrings/merobox.gpg > /dev/null
          echo "deb [signed-by=/usr/share/keyrings/merobox.gpg] https://calimero-network.github.io/merobox stable main" \
            | sudo tee /etc/apt/sources.list.d/merobox.list
          sudo apt-get update
          sudo apt-get install -y merobox
          merobox --version

      - name: Run Battleships E2E Workflow
        id: run_e2e
        run: |
          mkdir -p "$GITHUB_WORKSPACE/docker-logs"
          cd e2e
          max_attempts=2
          attempt=1
          success=false

          # Start a background watcher that streams `docker logs -f` from
          # each merod node container to an artifact file as soon as it
          # appears. A post-run `docker logs` loop does not work reliably on
          # current merobox because the runtime containers are torn down as
          # part of `merobox bootstrap run`'s exit path, leaving only the
          # init-container names behind by the time the shell regains
          # control. The watcher captures logs *while* containers are live,
          # so post-run cleanup can't remove what we've already written to
          # disk. Pattern lifted from calimero-network/core's e2e-rust-apps
          # workflow; tracked upstream in merobox#207 / core#2179.
          LOGDIR="$GITHUB_WORKSPACE/docker-logs"
          WATCHER_STATE_DIR="$(mktemp -d)"
          (
              while [ ! -f "$WATCHER_STATE_DIR/stop" ]; do
                  for c in $(docker ps --filter "label=calimero.node=true" --format "{{.Names}}" 2>/dev/null | grep -v -- '-init$' || true); do
                      mark="$WATCHER_STATE_DIR/${c}"
                      if [ ! -f "$mark" ]; then
                          touch "$mark"
                          echo "[log-watcher] following $c"
                          docker logs -f "$c" > "$LOGDIR/e2e-${c}.log" 2>&1 &
                      fi
                  done
                  sleep 1
              done
          ) &
          WATCHER_PID=$!

          while [ $attempt -le $max_attempts ]; do
              if [ $attempt -gt 1 ]; then
                  echo "Retry attempt $attempt..."
                  merobox stop --all || true
                  merobox nuke --force || true
                  sleep 2
              fi

              if merobox bootstrap run \
                  "workflow-battleships-e2e.yml" \
                  --e2e-mode; then
                  success=true
                  break
              else
                  attempt=$((attempt + 1))
              fi
          done

          # Signal the watcher to stop and give `docker logs -f` followers
          # a moment to flush their final output.
          touch "$WATCHER_STATE_DIR/stop"
          sleep 2
          kill "$WATCHER_PID" 2>/dev/null || true
          for pid in $(jobs -p); do kill "$pid" 2>/dev/null || true; done
          wait 2>/dev/null || true

          merobox stop --all || true
          merobox nuke --force || true

          if [ "$success" = false ]; then
              echo "failed=true" >> $GITHUB_OUTPUT
              exit 1
          fi

      - name: Upload Docker logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-docker-logs-${{ github.sha }}
          path: docker-logs/
          retention-days: 7
          if-no-files-found: ignore
